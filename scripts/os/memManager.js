/* ------------
   MemManager.js
   
   
   ------------ */

function memManager()
{
   //the memory will later be split into 3 sections
   //the memManager will keep track of each section and
   //keep processes from accessing memory outside their
   //section. However as of right now the memory has only
   //one section and loading a new program will simply
   //replace the old one.
}